# 维度一级导航明细文档

## 1. 文档说明

本文档整理敏捷自助分析页面中“维度选择区”的一级快速导航分类，以及每个一级分类下包含的具体维度。

字段说明：

- 一级导航：维度选择区左侧快速导航名称。
- 页面展示名称：维度行在页面上展示给用户的名称。
- 维度字段：页面配置中用于识别该维度的字段名。
- 默认状态：页面初始化时该维度是否默认展示。

## 2. 一级导航总览

维度一级快速导航包含以下分类：

1. 地域门店
2. 时段日期
3. 账单消费
4. 排号订餐
5. 外送客服
6. 会员标签
7. 菜品
8. 供应链
9. 营销活动
10. 财务
11. 人事

## 3. 各一级导航下具体维度

### 3.1 地域门店

| 序号 | 页面展示名称 | 维度字段 | 默认状态 |
| --- | --- | --- | --- |
| 1 | 大中华 | 地区 | 默认展示 |
| 2 | 省份/城市 | 城市 | 默认展示 |
| 3 | 地级市 | 地级市 | 默认隐藏 |
| 4 | 大陆城市线级 | 大陆城市线级 | 默认隐藏 |
| 5 | 大区经理/门店 | 门店 | 默认展示 |
| 6 | 门店属性 | 门店属性 | 默认隐藏 |
| 7 | 门店级别 | 门店级别 | 默认隐藏 |
| 8 | 营业状态 | 营业状态 | 默认隐藏 |

### 3.2 时段日期

| 序号 | 页面展示名称 | 维度字段 | 默认状态 |
| --- | --- | --- | --- |
| 1 | 星期 | 星期 | 默认隐藏 |
| 2 | 节假日属性 | 日期类型 | 默认隐藏 |
| 3 | 分小时 | 用户时段 | 默认隐藏 |
| 4 | 分时段 | 时段 | 默认展示 |

### 3.3 账单消费

| 序号 | 页面展示名称 | 维度字段 | 默认状态 |
| --- | --- | --- | --- |
| 1 | 就餐人数区间 | 就餐人数区间 | 默认展示 |
| 2 | 账单实收金额区间 | 账单实收金额区间 | 默认展示 |
| 3 | 账单折扣区间 | 账单折扣区间 | 默认展示 |
| 4 | 账单属性 | 账单属性 | 默认展示 |

### 3.4 排号订餐

| 序号 | 页面展示名称 | 维度字段 | 默认状态 |
| --- | --- | --- | --- |
| 1 | 排号订餐业务场景 | 排号订餐业务场景 | 默认展示 |
| 2 | 订餐渠道 | 订餐渠道 | 默认隐藏 |
| 3 | 排号渠道 | 排号渠道 | 默认隐藏 |
| 4 | 订餐/排号市别 | 订餐/排号市别 | 默认隐藏 |
| 5 | 排号桌型 | 排号桌型 | 默认隐藏 |
| 6 | 订餐桌型 | 订餐桌型 | 默认隐藏 |
| 7 | 候补状态 | 候补状态 | 默认隐藏 |

### 3.5 外送客服

| 序号 | 页面展示名称 | 维度字段 | 默认状态 |
| --- | --- | --- | --- |
| 1 | 外送交易渠道 | 外送交易渠道 | 默认隐藏 |
| 2 | 外送业态 | 外送业态 | 默认展示 |
| 3 | 客服热线组别 | 客服热线组别 | 默认隐藏 |
| 4 | 客服热线区域名称 | 客服热线区域名称 | 默认隐藏 |

### 3.6 会员标签

| 序号 | 页面展示名称 | 维度字段 | 默认状态 |
| --- | --- | --- | --- |
| 1 | 会员属性 | 会员属性 | 默认展示 |
| 2 | 消费时会员等级 | 会员类型 | 默认展示 |
| 3 | 最新会员等级 | 最新会员等级 | 默认隐藏 |
| 4 | 是否大学生人群 | 是否大学生人群 | 默认隐藏 |
| 5 | 人群属性 | 用户类型 | 默认展示 |
| 6 | 会员消费桌数 | 会员消费桌数 | 默认隐藏 |
| 7 | 会员消费账单数 | 会员消费账单数 | 默认隐藏 |
| 8 | 注册渠道 | 注册渠道 | 默认隐藏 |
| 9 | 客群消费属性 | 客群消费属性 | 默认展示 |

### 3.7 菜品

| 序号 | 页面展示名称 | 维度字段 | 默认状态 |
| --- | --- | --- | --- |
| 1 | 菜品分类/名称 | 菜品大类 | 默认展示 |
| 2 | 总部菜品规格 | 总部菜品规格 | 默认展示 |
| 3 | 财务菜品规格类型 | 财务菜品规格类型 | 默认展示 |
| 4 | 财务菜品规格 | 菜品二级品类 | 默认展示 |

### 3.8 供应链

| 序号 | 页面展示名称 | 维度字段 | 默认状态 |
| --- | --- | --- | --- |
| 1 | 品牌 | 品牌 | 默认展示 |
| 2 | 供应商 | 供应商 | 默认展示 |
| 3 | 供应链门店 | 供应链门店 | 默认展示 |
| 4 | 收货主体 | 收货主体 | 默认隐藏 |
| 5 | 采购组织 | 采购组织 | 默认隐藏 |
| 6 | 温层 | 温层 | 默认隐藏 |
| 7 | 物流分类 | 物流分类 | 默认展示 |
| 8 | 配送类型 | 配送类型 | 默认展示 |
| 9 | 物料一级 | 物料一级 | 默认隐藏 |
| 10 | 物料二级 | 物料二级 | 默认隐藏 |
| 11 | 物料三级 | 物料三级 | 默认隐藏 |
| 12 | 物料sku | 物料sku | 默认隐藏 |
| 13 | 承运商 | 承运商 | 默认隐藏 |
| 14 | 物流发起方 | 物流发起方 | 默认隐藏 |

### 3.9 营销活动

| 序号 | 页面展示名称 | 维度字段 | 默认状态 |
| --- | --- | --- | --- |
| 1 | 券模板ID编码 | 券模板ID编码 | 默认隐藏 |
| 2 | 券模板ID名称 | 券模板ID名称 | 默认隐藏 |
| 3 | 营销卡卡名称 | 营销卡卡名称 | 默认隐藏 |
| 4 | 一级优惠类型 | 一级优惠类型 | 默认隐藏 |
| 5 | 二级优惠类型 | 二级优惠类型 | 默认隐藏 |
| 6 | 三级优惠类型 | 三级优惠类型 | 默认隐藏 |
| 7 | 联名活动分类 | 联名活动分类 | 默认隐藏 |
| 8 | 活动名称 | 活动名称 | 默认隐藏 |
| 9 | 企业微信社群分类 | 企业微信社群分类 | 默认隐藏 |
| 10 | 企业微信会员等级 | 企业微信会员等级 | 默认隐藏 |
| 11 | 三方平台统一名称 | 三方平台统一名称 | 默认展示 |

### 3.10 财务

| 序号 | 页面展示名称 | 维度字段 | 默认状态 |
| --- | --- | --- | --- |
| 1 | 管报三级部门 | 管报三级部门 | 默认展示 |

### 3.11 人事

| 序号 | 页面展示名称 | 维度字段 | 默认状态 |
| --- | --- | --- | --- |
| 1 | 人事_员工类型 | 人事_员工类型 | 默认展示 |
| 2 | 人事_员工姓名 | 员工 | 默认展示 |
| 3 | 人事_是否正式工 | 人事_是否正式工 | 默认隐藏 |
| 4 | 人事_是否新员工 | 人事_是否新员工 | 默认隐藏 |
| 5 | 人事_休假类型 | 人事_休假类型 | 默认隐藏 |

## 4. 默认展示汇总

页面初始化时默认展示以下维度：

- 地域门店：大中华、省份/城市、大区经理/门店
- 时段日期：分时段
- 账单消费：就餐人数区间、账单实收金额区间、账单折扣区间、账单属性
- 排号订餐：排号订餐业务场景
- 外送客服：外送业态
- 会员标签：会员属性、消费时会员等级、人群属性、客群消费属性
- 菜品：菜品分类/名称、总部菜品规格、财务菜品规格类型、财务菜品规格
- 供应链：品牌、供应商、供应链门店、物流分类、配送类型
- 营销活动：三方平台统一名称
- 财务：管报三级部门
- 人事：人事_员工类型、人事_员工姓名

## 5. 说明

1. 默认隐藏维度不在页面初始维度列表中展示，需要通过“添加更多{一级导航名称}维度”入口添加。
2. 点击一级快速导航时，页面应定位到该分类下当前已展示的第一个维度。
3. 点击一级导航右侧小眼睛隐藏后，再次展示时应恢复上一次隐藏前实际展示的维度集合。
4. “展示全部维度”会展示以上所有一级分类下的全部维度。
