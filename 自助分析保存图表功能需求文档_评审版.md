# 敏捷自助分析「保存图表」功能需求文档

版本：V1.0 评审版  
日期：2026-05-18  
适用范围：敏捷自助分析、我的看板

## 1. 背景与目标

用户在敏捷自助分析完成指标查询、趋势分析、汇总均值或高阶汇总后，需要将当前分析结果沉淀到「我的看板」中，便于后续复用、查看、编辑查询条件、追加表格、下载、分享和协同。

本功能目标是提供一个统一的「保存图表」入口，将当前自助分析结果保存为看板内图表，并支持保存到已有看板或新增看板。

## 2. 用户角色

主要用户：经营分析人员、数据分析人员、门店/区域运营人员。

使用场景：

- 用户完成一次自助分析查询后，希望把当前图表保存到已有看板。
- 用户希望新建一个看板，并将当前图表保存到该看板下。
- 用户希望选择保存哪些模块，例如只保存趋势分析，或同时保存趋势分析、汇总均值、高阶汇总。
- 用户希望保存图表时设置日期更新机制，决定看板后续打开时日期是否自动滚动。
- 用户后续在「我的看板」中编辑已保存图表的查询条件或保存内容。

## 3. 功能入口

入口名称：保存图表  
入口位置：敏捷自助分析页面右侧固定工具栏。

入口可用条件：

- 页面已完成至少一次有效查询并已生成可保存内容。
- 若未查询直接点击保存，系统提示：请先查询后再保存。

点击入口后打开「保存到看板」弹窗。

## 4. 保存到看板弹窗

### 4.1 弹窗标题与操作

新增保存时：

- 标题：保存到看板
- 主按钮：确定
- 辅助按钮：取消
- 关闭方式：点击右上角关闭、点击取消、点击遮罩区域

编辑已保存图表时：

- 标题：编辑图表
- 主按钮：保存
- 辅助按钮：取消

### 4.2 所属看板

字段名称：所属看板  
字段类型：单选  
选项：

- 已有看板
- 新增看板

默认选中：已有看板。

切换规则：

- 选择「已有看板」时展示「看板名称」下拉选择框。
- 选择「新增看板」时展示「看板名称」输入框和「所属主题」选择框。

### 4.3 已有看板

字段名称：看板名称  
字段类型：可搜索下拉框  
内容来源：用户已有看板列表。

交互规则：

- 点击输入框展示已有看板列表。
- 输入关键词后按看板名称过滤。
- 点击某个看板后，输入框展示所选看板名称。

校验规则：

- 选择已有看板时，看板名称必填。
- 未选择时点击确定，提示：请选择看板名称。

保存规则：

- 将当前图表保存到所选看板。
- 图表所属主题沿用该看板已绑定主题。

### 4.4 新增看板

字段名称：看板名称  
字段类型：输入框  
占位提示：请输入自定义名称。

校验规则：

- 必填。
- 未填写时点击确定，提示：请输入看板名称。

保存规则：

- 若看板名称不存在，保存成功后将其加入用户看板列表。
- 若看板名称已存在，后续可按产品策略扩展为提示重名或保存到已有看板；当前原型按输入值保存。

## 5. 所属主题交互

所属主题仅在「新增看板」模式下展示。

字段名称：所属主题  
字段类型：可搜索下拉框  
占位提示：请选择所属主题。

### 5.1 初始态

点击输入框后展示已有主题列表：

- + 新增主题
- 经营分析
- 收入分析
- 外送分析
- 门店分析
- 利润分析
- 会员分析

规则：

- 「+ 新增主题」固定在下拉顶部。
- 主题列表按配置顺序展示。

### 5.2 搜索态

用户输入关键词后，下拉顶部新增主题文案带上当前输入内容。

示例：用户输入「会员」时，下拉展示：

- + 新增主题“会员”
- 会员分析
- 会员运营
- 会员消费

规则：

- 有匹配结果时，继续展示匹配主题。
- 「+ 新增主题」始终固定在顶部。
- 新增主题文案必须带上当前输入内容。

### 5.3 无匹配结果态

示例：用户输入「活动复盘」且没有匹配结果时，下拉展示：

- + 新增主题“活动复盘”
- 暂无相关主题

规则：

- 无匹配结果时展示空状态文案：暂无相关主题。
- 「+ 新增主题“输入内容”」仍固定在顶部。

### 5.4 点击新增主题

用户点击「+ 新增主题“活动复盘”」后：

- 不立即创建主题。
- 仅在当前弹窗中临时选中该主题。
- 输入框展示：活动复盘。
- 输入框右侧展示小标签：新。
- 「新」标签不使用边框样式，仅做轻量文本提示。

示例展示：活动复盘  新

临时状态清除规则：

- 用户重新输入主题关键词时，清除「新」标签。
- 用户选择已有主题时，清除「新」标签。
- 关闭弹窗时，清除临时新增主题状态。

### 5.5 确定后的主题保存逻辑

点击弹窗底部「确定」时：

1. 校验主题名称是否为空。
2. 判断主题是否已存在。
3. 如果不存在，先创建主题。
4. 主题创建成功后，创建或保存看板。
5. 看板保存成功后，保存图表。
6. 保存成功后关闭弹窗。

异常提示：

- 未选择所属主题：请选择所属主题。
- 主题名称为空或无效：请输入有效的主题名称。

## 6. 保存内容

字段名称：保存内容  
字段类型：多选复选框  
选项：

- 趋势分析
- 汇总均值
- 高阶汇总

默认状态：全部勾选。

规则：

- 用户可任意组合勾选。
- 至少需要勾选一项。
- 如果三项均未勾选，点击确定提示：请至少选择一项保存内容。

保存范围：

- 趋势分析：保存当前趋势图、趋势表格及相关展示变体。
- 汇总均值：保存当前汇总均值表和指标卡片。
- 高阶汇总：保存当前高阶汇总表和指标卡片。

## 7. 日期更新机制

字段名称：日期更新机制  
字段类型：单选  
默认选项：固定日期。

选项说明：

1. 固定日期
   - 查询日期不再变动。
   - 保存后看板内图表始终使用保存时的当前期日期范围。

2. 相同时间间隔滚动更新
   - 保持当前查询天数不变，随时间向前滚动。
   - 例如保存时为 7 天窗口，后续查看时仍保持 7 天窗口并随当前日期滚动。

3. 直接累积日期
   - 开始日期不变，结束日期随当前日期更新。
   - 适用于从固定起点开始持续累计的经营跟踪场景。

保存内容：

- 日期更新模式。
- 保存时当前期开始日期。
- 保存时当前期结束日期。
- 日期区间天数。
- 保存日期。

## 8. 图表名称

字段名称：图表名称  
字段类型：输入框  
占位提示：请输入自定义名称。

校验规则：

- 必填。
- 未填写时点击确定，提示：请输入图表名称。

保存规则：

- 图表名称作为我的看板中图表卡片标题。
- 后续支持在我的看板中编辑图表标题。

## 9. 查询条件展示

弹窗内展示当前待保存图表的查询条件，供用户确认。

展示内容：

- 当前期
- 对比期
- 分析指标

规则：

- 当前期展示当前查询的开始日期和结束日期。
- 对比期展示快捷词或具体日期范围。
- 分析指标展示当前查询指标，最多展示主要指标摘要。
- 查询条件为只读展示，不在保存弹窗中编辑。

## 10. 保存确认流程

### 10.1 保存到已有看板

流程：

1. 用户点击保存图表。
2. 打开保存到看板弹窗。
3. 默认选择已有看板。
4. 用户选择看板名称。
5. 用户选择保存内容。
6. 用户选择日期更新机制。
7. 用户输入图表名称。
8. 点击确定。
9. 系统校验必填项和保存内容。
10. 系统生成图表保存记录。
11. 图表写入所选看板。
12. 关闭弹窗。
13. 跳转或切换到我的看板，展示保存结果。
14. 提示：已保存到看板。

### 10.2 新增看板并保存

流程：

1. 用户点击保存图表。
2. 打开保存到看板弹窗。
3. 用户选择新增看板。
4. 用户输入看板名称。
5. 用户选择或临时新增所属主题。
6. 用户选择保存内容。
7. 用户选择日期更新机制。
8. 用户输入图表名称。
9. 点击确定。
10. 系统校验看板名称、所属主题、保存内容、图表名称。
11. 系统判断主题是否存在。
12. 主题不存在时先创建主题。
13. 创建看板并绑定主题。
14. 生成图表保存记录。
15. 写入看板。
16. 关闭弹窗。
17. 切换到我的看板，展示新增看板和图表。
18. 提示：已保存到看板。

## 11. 编辑已保存图表

入口：我的看板中图表标题右侧编辑图标。

点击后打开编辑图表弹窗：

- 标题为：编辑图表。
- 主按钮为：保存。
- 回填图表名称。
- 回填所属看板。
- 回填保存内容勾选状态。
- 回填日期更新机制。
- 回填查询条件摘要。

保存规则：

- 更新当前图表记录。
- 支持修改图表名称、所属看板、保存内容、日期更新机制。
- 取消某个保存内容后，对应模块在看板中不再展示。
- 保存成功后关闭弹窗并刷新我的看板。
- 提示：图表已更新。

## 12. 数据保存要求

保存图表记录至少包含：

- 图表 ID
- 图表名称
- 看板名称
- 所属主题
- 查询条件摘要
- 结构化查询状态
- 日期更新配置
- 保存内容配置
- 趋势分析快照
- 汇总均值快照
- 高阶汇总快照
- 创建时间
- 更新时间

所属主题与看板关系：

- 新增看板时绑定一个所属主题。
- 我的看板导航按主题聚合看板。
- 看板下展示该看板保存的图表。

## 13. 我的看板展示联动

保存成功后，我的看板需要同步展示：

- 左侧图表概览中新增或更新主题。
- 主题下新增或更新看板。
- 看板标题右侧显示图表数量。
- 看板右侧支持按图表名称搜索。
- 看板右侧支持分享看板入口。
- 图表卡片展示保存时选择的模块内容。

空状态：

- 没有保存内容时提示用户先在敏捷自助分析中查询并保存图表。
- 当前看板暂无图表时展示当前图表集暂无图表。
- 图表名称搜索无结果时展示未找到匹配图表。

## 14. 校验与提示汇总

| 场景 | 提示 |
| --- | --- |
| 未查询直接保存 | 请先查询后再保存 |
| 未选择已有看板 | 请选择看板名称 |
| 新增看板未输入名称 | 请输入看板名称 |
| 未选择所属主题 | 请选择所属主题 |
| 主题名称无效 | 请输入有效的主题名称 |
| 三项保存内容均未勾选 | 请至少选择一项保存内容 |
| 未输入图表名称 | 请输入图表名称 |
| 保存成功 | 已保存到看板 |
| 编辑保存成功 | 图表已更新 |

## 15. 非功能要求

性能要求：

- 打开保存弹窗响应时间不超过 500ms。
- 主题和看板下拉搜索应即时响应。
- 保存成功后我的看板刷新不应出现明显卡顿。

兼容要求：

- 支持已有保存记录继续展示。
- 旧数据存在三级主题路径时，我的看板展示取一级主题作为聚合主题。

可用性要求：

- 必填项需有明确提示。
- 新增主题在点击确定前不落库，避免误创建。
- 下拉中的新增主题入口始终固定在顶部。
- 保存内容默认全选，降低用户保存成本。

## 16. 验收标准

1. 未执行查询时点击保存图表，系统提示请先查询后再保存。
2. 点击保存图表后，能打开保存到看板弹窗。
3. 默认选择已有看板，并展示已有看板下拉。
4. 切换新增看板后，展示看板名称输入框和所属主题选择框。
5. 点击所属主题输入框后，下拉展示「+ 新增主题」和已有主题列表。
6. 输入「会员」后，下拉展示「+ 新增主题“会员”」「会员分析」「会员运营」「会员消费」。
7. 输入无匹配关键词「活动复盘」后，下拉展示「+ 新增主题“活动复盘”」和「暂无相关主题」。
8. 点击新增主题后，输入框展示活动复盘，右侧展示无边框的新标签，且此时不立即落库。
9. 点击确定后，若主题不存在，先创建主题，再保存看板和图表。
10. 保存成功后弹窗关闭，并在我的看板中能看到新增看板和图表。
11. 保存内容三项均取消时，点击确定提示请至少选择一项保存内容。
12. 日期更新机制能随图表记录保存，并在我的看板查询条件中展示对应日期更新文案。
13. 编辑已保存图表时能正确回填图表名称、保存内容、日期更新机制和查询条件摘要。
14. 编辑保存成功后，我的看板展示内容刷新。

## 17. 待评审问题

1. 新增看板名称是否需要做同主题下唯一性校验？
2. 新增主题是否需要区分个人主题和公共主题？
3. 主题创建失败时是否需要提供重试机制？
4. 日期更新机制是否需要在我的看板中提供二次编辑入口？
5. 保存内容取消后，是删除已保存快照还是仅隐藏展示？
6. 后续是否需要支持一个图表同时保存到多个看板？
