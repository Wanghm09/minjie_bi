# 维度与指标一级快速导航需求文档

## 1. 背景

敏捷自助分析页面的维度选择区和指标选择区内容较多，用户在筛选维度、选择指标时需要频繁上下滚动。为提升定位效率、控制默认展示范围，并支持供应链等新增业务域，本次对维度和指标选择区进行一级分类导航增强。

## 2. 目标

1. 在维度选择区和指标选择区增加一级快速导航，支持快速定位到对应分类。
2. 支持按一级分类隐藏或恢复维度/指标展示。
3. 支持页面滚动时，一级快速导航自动跟随当前展示内容切换高亮。
4. 新增供应链相关维度和指标，并按指定默认范围展示。
5. 将维度中的“管报三级部门”拆分为独立财务分组。

## 3. 范围

本需求适用于敏捷自助分析页面：

- 维度选择区
- 指标选择区
- 维度/指标搜索弹层
- 更多维度/更多指标弹层
- 选择区展开/收起交互

不包含：

- 后端数据模型改造
- 指标计算口径改造
- 查询结果表格和趋势图计算逻辑改造

## 4. 维度选择区需求

### 4.1 一级快速导航

维度选择区左侧展示一级快速导航，分类包括：

- 地域门店
- 时段日期
- 账单消费
- 排号订餐
- 外送客服
- 会员标签
- 菜品
- 供应链
- 营销活动
- 财务
- 人事

点击一级分类时，维度列表滚动到该分类下当前可见的第一条维度。

当前选中的一级分类使用蓝色文字、蓝色节点，并在名称右侧展示 `>` 箭头；未选中分类使用黑色文字。

### 4.2 供应链维度

新增供应链一级分类，包含以下维度：

- 品牌
- 供应商
- 供应链门店
- 收货主体
- 采购组织
- 温层
- 物流分类
- 配送类型
- 物料一级
- 物料二级
- 物料三级
- 物料sku
- 承运商
- 物流发起方

供应链默认展示维度：

- 品牌
- 供应商
- 供应链门店
- 物流分类
- 配送类型

供应链其他维度默认隐藏，通过“添加更多供应链维度”弹层选择展示。

### 4.3 财务维度

维度“管报三级部门”从原有分类中拆出，作为独立一级分类“财务”下的唯一维度。

财务维度默认展示：

- 管报三级部门

### 4.4 更多维度入口

每个一级分类下存在默认隐藏维度时，在该分类最后一个已展示维度行右侧展示入口：

`添加更多{一级分类名称}维度`

示例：

- 添加更多供应链维度
- 添加更多地域门店维度
- 添加更多时段日期维度

点击入口后，打开该一级分类下的更多维度弹层。

弹层支持：

- 搜索维度
- 勾选/取消勾选维度
- 全选当前搜索结果
- 清空当前一级分类已选维度

## 5. 指标选择区需求

### 5.1 一级快速导航

指标选择区左侧展示一级快速导航，分类包括：

- 经营
- 会员
- 菜品
- 供应链
- 人事
- 排号订餐
- 财务

指标快速导航放置在维度选择区与指标选择区之间的中间分割线上。

点击一级分类时，指标列表滚动到该分类下当前可见的第一条指标分组。

当前选中的一级分类使用蓝色文字、蓝色节点，并在名称右侧展示 `>` 箭头；未选中分类使用黑色文字。

指标快速导航名称、箭头和小眼睛之间应保持紧凑展示，减少横向占用。

### 5.2 供应链指标

新增供应链一级分类，包含以下指标分组：

#### 供应链_采购

全部指标：

- 采购金额
- 采购量
- top5采购金额集中度
- top20采购金额集中度
- 加权均价(基本单位)

默认展示：

- 采购金额
- 采购量
- 加权均价(基本单位)

#### 供应链_收货

全部指标：

- 收货量
- 收货金额
- 库存数量

默认展示：

- 收货量
- 收货金额
- 库存数量

#### 供应链_物流

全部指标：

- 物流费用总额
- 在途合格数(冷链断链)
- 在途总数(冷链断链)
- 配送车次数
- 配送晚点次数
- 配送次数
- 配送门店数
- 配送里程数
- 准时交付率(交付满足率)
- 冷链断链率(车温不合格率)

默认展示：

- 物流费用总额
- 在途合格数(冷链断链)
- 在途总数(冷链断链)

### 5.3 更多指标入口

当指标分组存在默认隐藏指标时，在该分组末尾展示“更多指标”入口。

点击入口后，打开该指标分组对应的更多指标弹层。

弹层支持：

- 搜索指标
- 勾选/取消勾选指标
- 全选当前搜索结果
- 清空当前指标分组已展示指标

## 6. 一级分类显隐控制

### 6.1 小眼睛入口

维度和指标的每个一级快速导航项右侧展示小眼睛图标。

小眼睛用于控制该一级分类下维度/指标的展示和隐藏。

默认状态：

- 小眼睛为黑色
- 一级导航文字为黑色
- 当前选中一级导航文字为蓝色

隐藏状态：

- 小眼睛显示斜线
- 一级分类文字置灰

### 6.2 显隐规则

点击小眼睛隐藏时：

- 隐藏当前一级分类下当前页面实际展示的维度/指标。
- 记录本次隐藏前该一级分类实际展示的维度/指标集合。

再次点击小眼睛展示时：

- 恢复上一次隐藏前实际展示的维度/指标集合。
- 不强制展示该一级分类下全部维度/指标。

示例：

- 供应链维度默认只展示 5 个维度。
- 用户点击供应链小眼睛隐藏后，再次点击展示，仍恢复这 5 个维度。
- 供应链其他隐藏维度仍通过“添加更多供应链维度”弹层添加。

### 6.3 总展示按钮

维度选择区保留“展示全部维度”按钮。

指标选择区新增“展示全部指标”按钮。

点击总展示按钮时：

- 维度：展示所有维度。
- 指标：展示所有指标。

当全部维度/指标均已展示时，按钮置为不可用状态，并显示：

- 已展示全部维度
- 已展示全部指标

## 7. 展开全部按钮

“展开全部”按钮位于维度选择区与指标选择区的中间分割线底部，并位于指标一级快速导航下方。

点击后：

- 展开维度和指标选择区高度。
- 再次点击可收起。

按钮位置应始终贴近两块选择区中间线，避免遮挡维度或指标内容。

## 8. 滚动联动需求

### 8.1 内部滚动联动

当维度列表或指标列表在选择区内部上下滚动时：

- 左侧一级快速导航应根据当前视口中展示的维度/指标自动切换高亮。
- 高亮分类应尽量对应当前滚动视口顶部附近正在展示的分类。

### 8.2 页面滚动联动

当选择区处于展开状态，页面整体上下滚动时：

- 维度一级快速导航和指标一级快速导航同样需要根据当前页面展示内容自动切换高亮。

### 8.3 显隐变化后的联动

当用户通过以下方式改变维度/指标可见状态后：

- 点击小眼睛隐藏/展示分类
- 点击展示全部维度
- 点击展示全部指标
- 在更多维度弹层中勾选/取消勾选
- 在更多指标弹层中勾选/取消勾选
- 恢复默认设置

系统应立即重新计算当前可见分类，并更新快速导航高亮。

## 9. 默认展示规则

### 9.1 维度默认展示

维度默认展示范围包含原有默认维度，并新增：

- 品牌
- 供应商
- 供应链门店
- 物流分类
- 配送类型
- 管报三级部门

供应链其他维度默认隐藏。

### 9.2 指标默认展示

指标默认展示范围为页面默认可见指标，并新增供应链默认指标：

- 供应链_采购：采购金额、采购量、加权均价(基本单位)
- 供应链_收货：收货量、收货金额、库存数量
- 供应链_物流：物流费用总额、在途合格数(冷链断链)、在途总数(冷链断链)

供应链其他指标默认隐藏。

## 10. 交互与视觉要求

1. 一级快速导航默认文字为黑色。
2. 当前选中一级快速导航为蓝色，并显示右侧 `>` 箭头。
3. 小眼睛默认黑色，hover 时可高亮。
4. 指标快速导航应贴近维度/指标中间分割线。
5. 指标快速导航中文字、箭头、小眼睛间距保持紧凑。
6. “展开全部”按钮位于中间分割线底部，在指标一级快速导航下方。
7. 更多维度入口保持蓝色链接样式。
8. 分类隐藏后，不应影响已记录的上次展示集合。

## 11. 验收标准

1. 维度选择区存在“供应链”和“财务”一级快速导航。
2. 指标选择区存在“供应链”一级快速导航。
3. “管报三级部门”只归属于维度一级分类“财务”。
4. 供应链维度默认只展示品牌、供应商、供应链门店、物流分类、配送类型。
5. 供应链指标默认展示内容与本需求第 5.2 节一致。
6. 点击一级快速导航，可滚动到对应分类。
7. 滚动维度/指标列表时，一级快速导航高亮自动变化。
8. 点击小眼睛隐藏后，再次点击只恢复上次展示过的内容，不强制展示全量。
9. 点击“展示全部维度”后，所有维度展示。
10. 点击“展示全部指标”后，所有指标展示。
11. “添加更多{一级分类名称}维度”入口文案正确。
12. 展开全部按钮位于维度/指标中线底部，不遮挡内容。
13. 快速导航默认黑色，选中项为蓝色并显示 `>`。

## 12. 待确认事项

1. “展示全部指标”是否需要与“恢复默认设置”区分权限或埋点。
2. 分类隐藏后，是否需要在页面刷新后保留用户上一次展示状态。
3. 供应链指标是否需要进一步区分数值型、比率型、周期型指标展示规则。
4. “财务”维度分类后续是否会继续扩展除“管报三级部门”以外的维度。
