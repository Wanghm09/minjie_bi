# 敏捷自助分析下载弹窗与下载中心需求文档

## 1. 文档信息与证据账本

| 项目 | 内容 |
| --- | --- |
| 需求名称 | 敏捷自助分析下载弹窗与下载中心 |
| 适用产品 | 敏捷自助分析 |
| 交付形态 | B 端数据分析页面、BI/报表导出、异步下载任务管理 |
| 原型来源 | `file:///D:/skilltohtml/0408-skilltohtml-prototype-iceblue-v9.html` |
| 读取范围 | 下载数据入口、下载任务创建弹窗、下载中心抽屉、任务通知、任务状态与操作逻辑 |
| 主要原型证据 | 顶部下载中心入口：HTML 约 1442-1450 行；底部下载数据/下载中心入口：约 1600-1603 行；下载中心抽屉：约 1607-1632 行；任务存储与状态：约 2973-3179 行；下载中心渲染与操作：约 3211-3309 行；创建任务弹窗：约 4418-4459、6667-6749 行；当前分析下载：约 20380-20405 行 |
| 文档版本 | v1.0 |
| 输出日期 | 2026-06-11 |

## 2. 背景与问题

敏捷自助分析支持用户在趋势分析、占比分析、对比分析中选择时间、维度和指标后查看分析结果。当前页面最多支持前台展示 15 条曲线或结果行。当用户选择的指标数、明细维度组合或查询结果超过前台展示上限时，继续在页面内同步渲染会影响用户等待体验，也可能导致页面展示不可读。

因此需要提供两类下载能力：

1. 结果未超限时，用户可直接下载当前分析结果文件。
2. 结果超限时，用户需创建异步下载任务，任务执行完成后在下载中心下载结果。

下载中心需要承接任务可见性、任务筛选、状态跟踪、下载、重试、暂停、删除、通知提醒等功能，确保用户离开当前查询上下文后仍可找到下载结果。

## 3. 业务目标

| 目标 | 说明 | 衡量方式 |
| --- | --- | --- |
| 降低大结果查询等待成本 | 超过前台展示上限的查询转为异步任务，用户无需停留等待页面渲染 | 超限查询均生成下载任务；创建任务成功后有明确反馈 |
| 保证下载任务可追踪 | 用户能在下载中心按查询类型、日期、状态、任务名称查找任务 | 下载中心能展示任务列表、查询条件、状态、创建时间和可执行操作 |
| 保证下载结果可获取 | 已完成任务能触发文件下载，并包含任务对应的查询条件与结果明细 | 已完成任务操作区出现“下载”；下载文件名与任务名称一致 |
| 保证任务状态可理解 | 用户能区分等待中、执行中、已完成、失败、已暂停、已过期等状态 | 状态文案、筛选项、操作项一致且可验收 |
| 保证个人数据隔离 | 下载任务仅当前登录用户可见 | 下载中心明确展示“仅当前用户可见”；不同用户不可互相查看任务 |

## 4. 用户角色与权限

| 角色 | 权限 | 边界 |
| --- | --- | --- |
| 当前登录用户 | 创建自己的下载任务、查看自己的下载任务、下载已完成文件、删除自己的任务、重试失败任务、暂停执行中任务 | 不可查看、下载、删除其他用户任务 |
| 系统任务执行服务 | 接收任务、执行查询、更新任务状态、生成下载文件、标记过期 | 不直接改变用户选择条件，不能将任务结果暴露给非创建人 |
| 产品/运营管理员 | 查看汇总监控、失败原因统计、容量与过期策略配置 | 默认不进入用户下载中心列表，若需代查需另行补权限需求 |

## 5. 范围

### 5.1 本期范围

1. 下载数据入口：在敏捷自助分析结果区提供“下载数据”入口。
2. 下载中心入口：在顶部用户区与页面底部操作区提供“下载中心”入口。
3. 直接下载：当前分析结果未超过前台展示上限时，直接生成并下载分析结果文件。
4. 异步下载任务创建：查询或下载触发超限时，创建下载任务并要求用户填写任务名称。
5. 下载中心列表：展示任务名称、创建人、查询类型、查询条件、创建时间、状态、操作。
6. 下载中心筛选：支持按查询类型、查询日期、任务状态、任务名称筛选，支持重置。
7. 任务操作：支持下载、删除、重试、暂停。
8. 通知提醒：任务创建成功、任务执行成功时展示 toast；下载中心入口展示未完成任务数量。
9. 空状态与分页：无任务时展示空状态；有任务时展示总数和分页控件。
10. 用户隔离：下载任务仅任务创建人可见。

### 5.2 本期不做

1. 不在下载中心内提供任务结果预览。
2. 不支持批量下载、批量删除。
3. 不支持跨用户共享下载任务。
4. 不支持用户自定义文件格式，默认下载 HTML 结果文件；如需 CSV/XLSX/PDF，另起需求。
5. 不支持手动编辑已提交任务的查询条件。

## 6. 术语定义

| 术语 | 定义 |
| --- | --- |
| 前台展示上限 | 页面可直接展示的最大曲线/结果数量，原型口径为 15 条 |
| 直接下载 | 不创建后台任务，基于当前页面已查询结果立即生成下载文件 |
| 异步下载任务 | 超出展示上限或用户从下载入口触发的大数据量下载任务，提交后进入下载中心 |
| 查询类型 | 趋势分析、占比分析、对比分析 |
| 查询条件 | 当前期、对比期、日期粒度、查询指标、明细维度组合等 |
| 任务状态 | 等待中、执行中、已完成、失败、已暂停、已过期 |

## 7. 关键业务规则

### 7.1 异步下载触发规则

| 编号 | 规则 |
| --- | --- |
| BR-01 | 当用户点击“查询”且预计结果超过前台展示上限时，不直接渲染结果，弹出“创建下载任务”弹窗。 |
| BR-02 | 当用户点击“下载数据”且当前分析结果超过前台展示上限时，不直接下载，创建异步下载任务。 |
| BR-03 | 超限判断至少包含两个口径：已选指标数是否大于 15；预计结果行/曲线数是否大于 15。 |
| BR-04 | 多选维度场景下，若用户选择“按单个维度值独立数据查询”，预计曲线数按“维度值数量 × 指标数量”计算。 |
| BR-05 | 查询未超限且页面已有查询结果时，点击“下载数据”应直接下载当前分析结果。 |
| BR-06 | 查询未超限但页面尚无查询结果时，点击“下载数据”应提示“请先查询后再下载”。 |

### 7.2 任务名称规则

| 编号 | 规则 |
| --- | --- |
| BR-07 | 创建异步下载任务时必须填写任务名称。 |
| BR-08 | 任务名称最长 50 个字符，超过后按前 50 个字符提交。 |
| BR-09 | 默认任务名称为“查询类型_yyyyMMdd_HHmmss”，例如“趋势分析_20260611_153012”。 |
| BR-10 | 任务名称为空时，弹窗内展示错误提示“请输入下载任务名称”，并阻止提交。 |

### 7.3 任务可见性规则

| 编号 | 规则 |
| --- | --- |
| BR-11 | 下载任务仅创建人可见。下载中心需展示“仅 {当前用户} 可见”或等价提示。 |
| BR-12 | 任务列表、任务下载、删除、暂停、重试均只能作用于当前用户自己的任务。 |
| BR-13 | 下载文件中可展示任务创建人的用户名、查询条件、指标和结果数据，但不得包含其他用户任务信息。 |

### 7.4 状态与操作规则

| 状态 | 展示文案 | 可执行操作 | 状态流转 |
| --- | --- | --- | --- |
| waiting | 等待中 | 暂停、删除 | 可进入 executing/processing、paused、expired |
| processing | 执行中 | 暂停、删除 | 可进入 done、failed、paused、expired |
| done | 已完成 | 下载、删除 | 可删除；文件过期后进入 expired |
| failed | 执行失败 | 重试、删除 | 重试后进入 processing |
| paused | 已暂停 | 删除；是否支持继续执行待确认 | 可删除；恢复操作本期未明确 |
| expired | 已过期 | 删除 | 可删除 |

待确认项：原型中“暂停”状态可展示和操作，但任务状态筛选下拉未包含“已暂停”。正式需求需补齐筛选项，或明确“已暂停”归入“执行中/异常”口径。

## 8. 业务流程

### 8.1 直接下载流程

1. 用户完成指标、时间、维度选择并点击查询。
2. 页面正常展示趋势图、明细表、汇总/均值、高阶汇总等结果。
3. 用户点击“下载数据”。
4. 系统判断当前结果未超过前台展示上限。
5. 系统生成“敏捷自助分析结果.html”。
6. 浏览器触发下载。
7. 页面展示“分析结果已下载”提示。

### 8.2 查询超限创建下载任务流程

1. 用户选择指标、时间、维度或多个对比项。
2. 用户点击“查询”。
3. 系统计算预计曲线/结果数量。
4. 若超过 15 条，系统打开“创建下载任务”弹窗。
5. 弹窗展示“当前最多支持 15 条曲线展示”和“本次预计生成 N 条曲线”。
6. 弹窗展示下载任务名称输入框，默认填入“查询类型_时间戳”。
7. 用户确认提交。
8. 系统创建任务，状态为“执行中”或“等待中”。
9. 系统展示“异步下载任务创建成功，由于下载数据量较大，请在下载中心查看”。
10. 用户可点击 toast 或下载中心入口进入下载中心。
11. 任务完成后，系统展示“下载任务执行成功”，并在下载中心展示“下载”操作。

### 8.3 下载数据超限创建任务流程

1. 用户已完成查询，点击“下载数据”。
2. 系统判断当前结果超过前台展示上限。
3. 系统创建异步下载任务，任务来源为“下载数据”。
4. 系统展示创建成功提示。
5. 用户进入下载中心查看任务状态和下载结果。

### 8.4 下载中心管理流程

1. 用户从顶部“下载中心”或底部“下载中心”入口进入下载中心。
2. 系统展示当前用户的下载任务列表。
3. 用户可按查询类型、查询日期、任务状态、任务名称筛选任务。
4. 用户可点击“重置”清空筛选条件。
5. 对已完成任务，用户可点击“下载”获取结果文件。
6. 对失败任务，用户可点击“重试”重新提交。
7. 对等待中/执行中任务，用户可点击“暂停”。
8. 对任意可删除状态任务，用户可点击“删除”移出列表。
9. 用户点击关闭按钮或遮罩区域关闭下载中心。

## 9. 页面与交互需求

### 9.1 下载数据入口

| 编号 | 需求 |
| --- | --- |
| UI-01 | 当敏捷自助分析存在分析结果或存在下载任务时，底部操作区展示“下载数据”和“下载中心”。 |
| UI-02 | “下载数据”入口使用下载图标和文字，点击后执行直接下载或异步任务创建判断。 |
| UI-03 | 未查询时点击“下载数据”，展示轻提示“请先查询后再下载”。 |

### 9.2 创建下载任务弹窗

| 编号 | 需求 |
| --- | --- |
| UI-04 | 弹窗标题为“创建下载任务”。 |
| UI-05 | 弹窗展示超限原因，至少包括前台展示上限和预计生成数量。 |
| UI-06 | 弹窗展示黄色/警示文案：“已超过页面展示上限，确认后将创建异步下载任务，完成后可在下载中心下载。” |
| UI-07 | 弹窗提供“下载任务名称”输入框，支持输入 1-50 个字符。 |
| UI-08 | 输入框右侧展示字数统计，例如“12 / 50”。 |
| UI-09 | 提交按钮文案为“提交下载任务”。 |
| UI-10 | 任务名称为空时展示错误，不提交任务。 |
| UI-11 | 用户关闭弹窗后，不创建任务，已输入内容不需要保留。 |

### 9.3 下载中心抽屉

| 编号 | 需求 |
| --- | --- |
| UI-12 | 下载中心以右侧抽屉形式打开，标题为“下载中心”。 |
| UI-13 | 抽屉顶部提供关闭按钮。 |
| UI-14 | 筛选区包含查询类型、查询日期起止、任务状态、任务名称关键字、重置、查询。 |
| UI-15 | 查询类型选项包括全部、趋势分析、占比分析、对比分析。 |
| UI-16 | 任务状态选项包括全部、等待中、执行中、已完成、失败、已暂停、已过期。 |
| UI-17 | 任务名称输入框 placeholder 为“请输入任务名称”。 |
| UI-18 | 列表字段包括任务名称、查询类型、查询条件、创建时间、状态、操作。 |
| UI-19 | 任务名称列同时展示创建人。 |
| UI-20 | 查询条件列展示当前期、对比期、查询指标；若有日期粒度和明细维度，应在正式实现中补充展示。 |
| UI-21 | 无任务或筛选无结果时展示“暂无下载任务”。 |
| UI-22 | 有任务时展示总数和分页控件，默认每页 10 条，支持切换 20 条/页。 |

### 9.4 通知与红点

| 编号 | 需求 |
| --- | --- |
| UI-23 | 顶部“下载中心”入口展示未完成任务数，统计范围为 waiting 和 processing。 |
| UI-24 | 底部“下载中心”入口展示未完成任务数，超过 9 展示“9+”。 |
| UI-25 | 顶部红点超过 99 展示“99+”。 |
| UI-26 | 创建任务成功后展示 toast，点击 toast 打开下载中心。 |
| UI-27 | 任务完成后展示 toast：“下载任务执行成功，具体任务名称【任务名】执行成功，请在下载中心进行下载数据”。 |
| UI-28 | toast 支持关闭按钮，点击关闭后仅关闭提示，不打开下载中心。 |
| UI-29 | 已完成任务应进入系统通知列表，通知文案包含查询类型、任务名称、完成时间。 |
| UI-30 | 通知支持标记已读；点击通知应进入下载中心。 |

## 10. 数据字段需求

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| task_id | 是 | 下载任务唯一标识 |
| owner | 是 | 任务创建人 |
| title | 是 | 任务名称，1-50 字符 |
| analysis_type | 是 | 趋势分析、占比分析、对比分析 |
| source | 是 | 查询、下载数据或其他触发来源 |
| reason | 是 | 创建异步任务原因，例如超过前台展示上限 |
| status | 是 | waiting、processing、done、failed、paused、expired |
| created_at | 是 | 创建时间 |
| finished_at | 否 | 完成时间 |
| current_period | 是 | 当前期 |
| compare_period | 否 | 对比期，无对比时展示“无” |
| date_grain | 是 | 日期粒度，例如按日、按周、按月 |
| metrics_text | 是 | 查询指标展示文本 |
| query_text | 是 | 查询条件完整描述 |
| row_count | 是 | 预计或实际结果行/曲线数 |
| metric_count | 是 | 指标数量 |
| data_names | 否 | 下载文件中的数据名称列表 |
| has_detail | 否 | 是否包含明细维度 |
| unread | 是 | 是否未读通知 |
| expire_at | 否 | 文件过期时间 |
| failure_reason | 否 | 失败原因，失败状态时必填 |

## 11. 下载文件内容需求

### 11.1 直接下载文件

| 编号 | 需求 |
| --- | --- |
| FILE-01 | 默认文件名为“敏捷自助分析结果.html”。 |
| FILE-02 | 文件内容包含页面当前已展示的趋势分析、汇总/均值、高阶汇总等结果模块。 |
| FILE-03 | 文件中隐藏页面操作按钮，避免下载后出现无效交互。 |
| FILE-04 | 文件编码为 UTF-8，保证中文可读。 |

### 11.2 异步任务下载文件

| 编号 | 需求 |
| --- | --- |
| FILE-05 | 文件名以任务名称命名，非法文件名字符需替换为下划线。 |
| FILE-06 | 文件标题展示任务名称。 |
| FILE-07 | 文件元信息展示用户、当前期、对比期、指标。 |
| FILE-08 | 文件主体展示结果表格，至少包含序号、数据名称、示例值/结果值、变动幅度。 |
| FILE-09 | 若正式数据包含更多维度或指标列，应按照查询条件完整导出，不得只导出页面可见前 15 条。 |

## 12. 异常与边界场景

| 场景 | 处理 |
| --- | --- |
| 未查询点击下载 | 提示“请先查询后再下载” |
| 任务名称为空 | 弹窗内报错，不提交 |
| 任务名称超过 50 字 | 提交时截断到 50 字，建议输入框同步限制 |
| 下载中心无任务 | 展示“暂无下载任务”，隐藏表格和分页 |
| 筛选无结果 | 展示“暂无下载任务”，保留筛选条件 |
| 任务执行失败 | 状态展示“执行失败”，操作展示“重试”和“删除”，失败原因应可查看或至少进入任务详情 |
| 任务重试 | 状态重新变为“执行中”，成功后更新完成时间并通知用户 |
| 任务暂停 | 状态变为“已暂停”，是否支持恢复待确认 |
| 任务过期 | 状态展示“已过期”，不允许下载，仅允许删除 |
| 任务被删除 | 从当前用户任务列表移除；若任务正在执行，需要同步取消或忽略后续结果 |
| 浏览器下载被拦截 | 页面提示用户允许下载或重新点击下载 |
| 同名任务 | 允许同名，系统以 task_id 区分；下载文件若同名由浏览器处理 |

## 13. 验收标准

| 编号 | Given | When | Then |
| --- | --- | --- | --- |
| AC-01 | 用户未完成查询 | 点击“下载数据” | 系统提示“请先查询后再下载”，不创建任务 |
| AC-02 | 用户已查询且结果未超 15 条 | 点击“下载数据” | 浏览器下载“敏捷自助分析结果.html”，页面提示“分析结果已下载” |
| AC-03 | 用户点击查询且预计结果超过 15 条 | 点击查询 | 打开“创建下载任务”弹窗，展示预计生成数量和任务名称输入框 |
| AC-04 | 创建任务弹窗中任务名称为空 | 点击“提交下载任务” | 展示“请输入下载任务名称”，任务不创建 |
| AC-05 | 创建任务弹窗中填写合法名称 | 点击“提交下载任务” | 任务创建成功，状态为等待中或执行中，下载中心可见 |
| AC-06 | 用户创建任务成功 | 查看页面 | 出现任务创建成功 toast，点击 toast 打开下载中心 |
| AC-07 | 任务执行完成 | 系统更新任务状态 | 下载中心状态为“已完成”，操作区展示“下载”和“删除” |
| AC-08 | 已完成任务 | 点击“下载” | 下载以任务名称命名的结果文件，文件包含用户、当前期、对比期、指标和结果表格 |
| AC-09 | 失败任务 | 点击“重试” | 任务状态变为“执行中”，重试成功后变为“已完成” |
| AC-10 | 执行中任务 | 点击“暂停” | 任务状态变为“已暂停”，列表操作按暂停状态展示 |
| AC-11 | 任一可删除任务 | 点击“删除” | 任务从列表移除，下载中心总数更新 |
| AC-12 | 下载中心输入任务名称关键字 | 点击“查询”或按 Enter | 列表仅展示任务名称命中的任务 |
| AC-13 | 下载中心选择查询类型 | 切换筛选项 | 列表仅展示对应趋势/占比/对比任务 |
| AC-14 | 下载中心选择日期范围 | 点击查询 | 列表仅展示创建日期在范围内的任务 |
| AC-15 | 下载中心点击“重置” | 已有筛选条件 | 清空查询类型、日期、状态、关键词并刷新列表 |
| AC-16 | 当前用户 A 创建任务 | 用户 B 登录下载中心 | 用户 B 不可见用户 A 的任务 |
| AC-17 | 未完成任务数量为 10 | 查看底部入口 | 底部下载中心角标展示“9+” |
| AC-18 | 未完成任务数量为 100 | 查看顶部入口 | 顶部下载中心角标展示“99+” |

## 14. 指标与监控

| 指标 | 口径 | 用途 |
| --- | --- | --- |
| 下载任务创建量 | 每日创建的异步下载任务数 | 判断功能使用量和容量压力 |
| 任务成功率 | 已完成任务数 / 创建任务数 | 监控异步任务稳定性 |
| 平均任务完成时长 | finished_at - created_at | 评估用户等待体验 |
| 任务失败原因分布 | 按 failure_reason 统计 | 定位数据源、权限、容量问题 |
| 下载点击率 | 已完成任务下载次数 / 已完成任务数 | 判断用户是否能找到结果 |
| 过期未下载量 | 过期且未下载任务数 | 判断过期策略是否合理 |
| 下载中心筛选使用率 | 使用筛选的访问次数 / 下载中心访问次数 | 判断筛选能力是否必要且易用 |

## 15. 发布、兼容与回滚

| 项目 | 要求 |
| --- | --- |
| 发布范围 | 先在敏捷自助分析模块上线；如其他 BI 模块复用，需补充模块来源字段和权限口径 |
| 灰度策略 | 可按用户或组织灰度开启异步下载任务 |
| 兼容策略 | 已存在的同步下载继续可用；超限场景才转异步 |
| 过期策略 | 下载文件建议配置有效期，默认值待业务确认；过期后任务保留但不可下载 |
| 回滚策略 | 若异步任务服务异常，可临时关闭超限任务提交，并提示“当前下载任务服务不可用，请稍后重试” |

## 16. 待确认事项

| 编号 | 问题 | 建议确认方 | 阻塞程度 |
| --- | --- | --- | --- |
| Q-01 | 正式前台展示上限是否固定为 15，还是按分析类型/用户权限配置 | 产品、研发 | 高 |
| Q-02 | 下载文件格式是否仅 HTML，是否需要 XLSX/CSV | 产品、业务用户 | 中 |
| Q-03 | 任务文件有效期为多久，过期前是否提醒 | 产品、运营 | 高 |
| Q-04 | “已暂停”状态是否需要支持“继续执行” | 产品、研发 | 中 |
| Q-05 | 失败任务是否需要展示失败原因详情 | 产品、研发、客服 | 中 |
| Q-06 | 下载中心分页是否需要真实翻页和页大小切换，本期原型仅展示控件 | 产品、研发 | 中 |
| Q-07 | 管理员是否需要代查用户下载任务 | 业务负责人、权限负责人 | 中 |
| Q-08 | 下载文件中是否包含敏感指标，是否需要水印、审计或脱敏 | 安全、数据治理 | 高 |

## 17. 架构门禁自检

| Gate 项 | 当前覆盖情况 |
| --- | --- |
| 业务目标与背景 | 已说明大结果查询、直接下载与异步任务的业务问题和目标 |
| 范围与非范围 | 已明确本期做下载入口、弹窗、下载中心、通知、任务操作；排除批量、共享、自定义格式 |
| 角色与权限 | 已定义当前用户、任务执行服务、管理员，并明确仅创建人可见 |
| 主流程 | 已覆盖直接下载、查询超限创建任务、下载超限创建任务、下载中心管理 |
| 数据口径 | 已定义任务字段、查询条件、展示上限、状态、文件内容 |
| 异常处理 | 已覆盖未查询、空任务名、失败、暂停、过期、删除、浏览器拦截等场景 |
| 验收标准 | 已提供可测试的 Given/When/Then 验收用例 |
| 安全与隐私 | 已明确用户隔离、下载文件边界，并列出敏感数据待确认 |
| 发布与回滚 | 已提供灰度、兼容、过期、回滚口径 |

