# 门店弹窗常用筛选组合功能需求文档

## 1. 文档说明

本文基于原型文件 `D:\skilltohtml\0408-skilltohtml-prototype-iceblue-v9.html` 中“门店筛选”弹窗及其“常用筛选组合”交互整理，作为后续产品、设计、前后端开发和测试的需求依据。

## 2. 功能背景

在敏捷 BI 查询场景中，用户经常需要按固定门店集合查询指标，例如“华东核心门店”“京津重点门店”等。若每次都按大区经理逐个筛选门店，操作成本较高且容易漏选。常用筛选组合用于保存一组已选门店，并在后续查询中一键复用。

## 3. 目标

- 支持用户将当前已选门店保存为常用筛选组合。
- 支持在门店筛选弹窗内快速查看、预览并应用已保存组合。
- 支持跨大区经理累计选择后的门店组保存和复用。
- 降低重复筛选成本，提升 BI 查询条件配置效率。

## 4. 使用入口

入口路径：敏捷 BI 页面 > 维度选择 > 门店 > 添加筛选门店。

打开后展示“门店筛选”侧滑弹窗，弹窗内包含：

- 左侧待选区：大区经理单选、门店搜索、门店多选/单选列表。
- 左侧已选区：展示当前已选门店，支持清空和移除单个门店。
- 右侧常用筛选组合区：展示已保存的门店组合。
- 底部操作区：取消、确认。

## 5. 页面结构

### 5.1 常用筛选组合区

位置：门店弹窗右侧。

展示内容：

- 标题：常用筛选组合。
- 组合列表：每个组合展示组合名称和门店数量。
- 空态：无组合时展示“暂无常用组合”。

原型内置示例：

- 华东核心门店，3 项。
- 京津重点门店，2 项。

### 5.2 保存组合入口

位置：已选区标题栏右侧。

按钮文案：添加为常用筛选组合。

触发条件：

- 当前已选择至少 1 个门店时，点击后打开保存弹窗。
- 当前未选择门店时，不打开弹窗，并提示“请先选择门店”。

### 5.3 保存组合弹窗

弹窗标题：保存为常用筛选组合。

字段与内容：

- 组合名称输入框，placeholder 为“请输入组合名称”。
- 预览标题：当前已选门店。
- 预览区：以标签形式展示当前已选的全部门店。
- 操作按钮：取消、保存。

交互要求：

- 打开弹窗时组合名称默认为空。
- 打开弹窗后自动聚焦到组合名称输入框。
- 点击遮罩或取消按钮关闭弹窗，不保存数据。
- 点击保存按钮执行保存校验。

## 6. 功能需求

### FR-01 展示常用筛选组合

打开门店弹窗时，系统应加载并展示用户可用的常用筛选组合。

列表项展示规则：

- 左侧展示组合名称。
- 右侧展示该组合包含的门店数量，格式为“n项”。
- 新保存的组合展示在列表顶部。

### FR-02 保存当前门店为组合

用户点击“添加为常用筛选组合”后，系统应将当前已选门店作为待保存内容。

保存校验：

- 未选择门店：提示“请先选择门店”。
- 未输入组合名称：提示“请输入组合名称”。
- 组合名称需去除首尾空格。

保存成功后：

- 关闭保存弹窗。
- 将新组合插入常用组合列表顶部。
- 当前已选门店状态保持不变。

重名处理：

- 若组合名称已存在，原型采用自动追加序号的方式生成新名称，例如 `华东核心门店(2)`、`华东核心门店(3)`。
- 正式实现建议沿用该规则，或在产品确认后改为阻断保存并提示重名。

### FR-03 预览当前保存内容

保存弹窗中必须展示当前将被保存的门店范围。

展示规则：

- 每个门店以标签形式展示。
- 预览区支持滚动，避免门店数量过多时撑开弹窗。
- 预览内容应与当前已选区保持一致。

### FR-04 应用常用筛选组合

用户点击右侧常用筛选组合列表中的某一项后，系统应立即将该组合应用到当前弹窗内的门店选择状态。

应用规则：

- 用组合内门店替换当前已选门店，而不是追加。
- 已选区刷新为组合内全部门店。
- 门店待选区中属于当前大区经理的门店应同步勾选。
- 若组合首个门店归属某个大区经理，弹窗左侧大区经理选中状态切换到该经理。
- 跨大区经理的门店仍需保留在已选区中。

### FR-05 悬浮查看组合明细

用户鼠标悬浮在组合项上时，应展示组合包含的完整门店列表。

展示规则：

- 浮层跟随鼠标位置展示。
- 每行展示一个门店。
- 浮层应限制最大宽度和高度，超出时支持滚动。
- 鼠标移出组合项后隐藏浮层。

### FR-06 与门店基础筛选联动

常用筛选组合需要与门店弹窗内已有筛选能力联动。

联动规则：

- 大区经理为单选，用于过滤待选门店列表。
- 门店搜索仅过滤当前大区经理下的可见门店，不影响已选门店。
- 已选区支持跨大区经理累计门店。
- 点击单个已选门店标签的关闭符号，可移除该门店。
- 点击“清空已选”后，清空所有已选门店，并刷新待选区勾选状态。

### FR-07 与菜品多选约束联动

原型提示“门店和菜品仅支持其中一个按照多选查询明细”。

规则要求：

- 当菜品已多选时，门店选择应限制为单选。
- 当门店已多选时，菜品选择应限制为单选。
- 应用多门店组合时，如与已选菜品多选状态冲突，系统需按约束收敛选择状态。
- 建议正式实现增加冲突提示，避免用户感知为静默丢失选择。

### FR-08 确认与取消

门店弹窗底部提供“取消”和“确认”。

确认：

- 将当前弹窗内的门店选择同步到主页面筛选摘要。
- 主页面门店摘要展示格式为“首个门店等n项”。
- 触发相关查询结果或趋势明细的刷新。

取消/关闭：

- 关闭门店弹窗。
- 不应更新主页面筛选摘要。
- 正式实现建议使用弹窗临时态，避免取消后影响已生效筛选条件。

## 7. 数据要求

正式实现建议使用结构化数据保存组合。

```json
{
  "id": "combo_id",
  "type": "store",
  "name": "华东核心门店",
  "items": [
    {
      "storeId": "store_id",
      "storeName": "上海一店"
    }
  ],
  "itemCount": 1,
  "ownerUserId": "user_id",
  "createdAt": "2026-05-06T00:00:00+08:00",
  "updatedAt": "2026-05-06T00:00:00+08:00"
}
```

说明：

- 原型中组合仅保存在前端内存中，刷新页面后不会保留。
- 正式系统需明确持久化范围：个人私有、角色共享、组织共享或系统预置。
- 门店应以门店 ID 作为主键，门店名称仅用于展示，避免门店重名导致筛选错误。

## 8. 边界场景

- 保存空组合：不允许保存，提示用户先选择门店。
- 保存空名称：不允许保存，提示用户输入组合名称。
- 保存重名组合：按序号自动生成唯一名称，或按产品确认改为阻断。
- 组合内门店数量较多：列表项只展示数量，完整内容通过悬浮浮层展示。
- 组合包含已停用或无权限门店：正式实现需过滤不可用门店，并提示部分门店不可用。
- 搜索无匹配门店：待选区展示“未匹配到门店”。
- 当前大区经理暂无门店：待选区展示“该大区经理暂无门店”。

## 9. 非功能需求

- 性能：常用组合列表和门店预览在百级门店数量下应保持流畅滚动。
- 易用性：组合保存后应即时出现在列表顶部，减少用户二次查找。
- 可读性：组合列表默认只展示名称和数量，避免右侧区域过载。
- 可访问性：保存弹窗打开后聚焦输入框；关闭、保存、取消按钮需可键盘操作。
- 安全性：组合名称和门店名称展示时需做 HTML 转义，避免脚本注入。

## 10. 验收标准

- 已选择多个门店时，点击“添加为常用筛选组合”会打开保存弹窗，并展示全部已选门店。
- 未选择门店时，点击保存入口提示“请先选择门店”。
- 未输入组合名称点击保存时，提示“请输入组合名称”。
- 保存成功后，新组合出现在右侧组合列表顶部，数量与已选门店数一致。
- 保存重名组合时，系统能生成唯一名称或给出明确重名提示。
- 点击组合项后，已选区被替换为该组合内门店。
- 悬浮组合项时，可以看到该组合包含的完整门店列表。
- 移出组合项后，明细浮层隐藏。
- 点击确认后，主页面门店筛选摘要更新为“首个门店等n项”。
- 点击取消或关闭时，主页面筛选摘要不更新。

## 11. 待确认问题

- 常用组合是个人私有，还是支持团队/角色共享。
- 是否需要支持编辑组合名称、删除组合、调整组合排序。
- 单个用户最多可保存多少个门店组合。
- 单个组合最多允许包含多少个门店。
- 与菜品多选冲突时，是自动收敛选择，还是弹窗二次确认。
- 组合内门店失效、无权限或名称变更时的提示策略。
