# 一级快速导航功能需求文档

## 1. 功能点总结

一级快速导航用于提升维度选择区和指标选择区的浏览、定位和显隐控制效率。页面左侧或中间分割线侧展示一级分类导航，用户可以通过点击一级分类快速滚动到对应维度或指标分组；也可以通过分类右侧的小眼睛控制该一级分类下当前展示内容的隐藏与恢复。

本次新增和调整的核心功能点如下：

1. 维度选择区和指标选择区均支持一级快速导航。
2. 一级导航支持点击定位到对应分类内容。
3. 页面或列表滚动时，一级导航高亮自动跟随当前展示内容变化。
4. 每个一级导航项右侧增加小眼睛，用于隐藏或恢复该一级分类下的内容。
5. 小眼睛恢复时只恢复上一次隐藏前实际展示的内容，不强制展示全量。
6. 维度选择区和指标选择区均支持一键展示全部。
7. 选中的一级导航项使用原蓝色，并展示右侧箭头 `>`。
8. 未选中的一级导航项、小眼睛默认使用黑色。
9. 指标一级快速导航放置在维度区和指标区之间的中间线上。
10. “展开全部”按钮放置在中间分割线底部，并位于指标一级快速导航下方。
11. 新增供应链一级分类，并分别承载供应链相关维度和指标。
12. 维度中“管报三级部门”独立为财务分组，一级快速导航为“财务”。

## 2. 背景

敏捷自助分析页面中，维度和指标数量较多。用户在配置分析条件时，需要频繁滚动查找某类维度或指标，尤其在新增供应链相关内容后，页面信息密度进一步增加。

为降低查找成本、增强分类感知，并支持用户按业务域控制内容展示，需要对维度选择区和指标选择区增加一级快速导航能力。

## 3. 目标

1. 用户能够通过一级快速导航快速定位到目标业务分类。
2. 用户能够按一级分类隐藏或恢复维度/指标展示。
3. 页面滚动时，导航高亮能够准确反映当前正在浏览的业务分类。
4. 默认展示范围保持简洁，更多内容通过“添加更多维度/指标”补充。
5. 展示全部、恢复默认、更多弹窗、小眼睛显隐之间的状态保持一致。

## 4. 适用范围

本需求适用于以下区域：

- 维度选择区
- 指标选择区
- 维度一级快速导航
- 指标一级快速导航
- 更多维度弹窗
- 更多指标弹窗
- 展示全部维度/指标按钮
- 展开全部/收起按钮

不包含以下范围：

- 后端指标计算逻辑
- 查询结果表格渲染逻辑
- 图表展示逻辑
- 用户权限体系
- 页面刷新后的持久化存储，除非后续另行定义

## 5. 角色与使用场景

### 5.1 业务分析用户

用户在配置分析条件时，希望快速找到某一类维度或指标，例如供应链、菜品、会员、财务等。

典型场景：

1. 用户点击“供应链”，页面快速定位到供应链维度或指标。
2. 用户暂时不关注“人事”分类，点击小眼睛隐藏人事相关维度/指标。
3. 用户滚动浏览指标时，左侧快速导航自动切换到当前浏览分类。
4. 用户需要完整配置时，点击“展示全部维度”或“展示全部指标”。

## 6. 维度一级快速导航需求

### 6.1 导航分类

维度选择区展示一级快速导航，分类包括：

- 地域门店
- 时段日期
- 账单消费
- 排号订餐
- 外送客服
- 会员标签
- 菜品
- 供应链
- 营销活动
- 财务
- 人事

其中：

- “供应链”为新增维度一级分类。
- “财务”为新增维度一级分类。
- “管报三级部门”应归属到“财务”分类下，作为独立分组展示。

### 6.2 导航位置

维度一级快速导航展示在维度选择区左侧。

导航应与维度内容区域保持视觉关联，用户可以明确感知当前导航控制的是维度选择区。

### 6.3 点击定位

用户点击任意维度一级导航项时：

1. 维度列表滚动到该分类下当前可见的第一条维度。
2. 被点击的一级导航项变为选中状态。
3. 如果该分类当前无可见维度，则不滚动内容区，但导航显隐状态仍保持正确。
4. 如果该分类被小眼睛隐藏，点击分类名称不应强制展开该分类。

### 6.4 默认展示

维度默认展示应保持原有默认范围，并新增供应链默认展示维度：

- 品牌
- 供应商
- 供应链门店
- 物流分类
- 配送类型

“管报三级部门”作为财务分类下维度默认展示。

供应链其他维度默认隐藏，需要通过“添加更多供应链维度”弹窗添加。

### 6.5 更多维度入口

每个一级分类如存在未展示维度，需要提供“添加更多{一级分类名称}维度”入口。

示例：

- 添加更多供应链维度
- 添加更多菜品维度
- 添加更多地域门店维度

点击入口后：

1. 打开对应一级分类的更多维度弹窗。
2. 弹窗只展示当前一级分类下的维度。
3. 用户可以勾选更多维度并保存。
4. 保存后，维度列表立即更新。
5. 一级快速导航高亮状态重新计算。

## 7. 指标一级快速导航需求

### 7.1 导航分类

指标选择区展示一级快速导航，分类包括：

- 经营
- 会员
- 菜品
- 供应链
- 人事
- 排号订餐
- 财务

其中“供应链”为新增指标一级分类。

### 7.2 导航位置

指标一级快速导航应放置在维度选择区和指标选择区之间的中间分割线上。

视觉要求：

1. 导航线与中间分割线对齐。
2. 导航内容不应遮挡指标文本。
3. 导航名称、右侧箭头和小眼睛之间应保持紧凑。
4. 导航整体应比指标正文更靠近分割线，体现其为左侧锚点导航。

### 7.3 点击定位

用户点击任意指标一级导航项时：

1. 指标列表滚动到该分类下当前可见的第一个指标分组。
2. 被点击的一级导航项变为选中状态。
3. 如果该分类当前没有可见指标分组，则不滚动内容区。
4. 如果该分类被隐藏，点击分类名称不应自动恢复该分类。

### 7.4 供应链指标默认展示

供应链指标分为以下分组：

#### 供应链_采购

默认展示：

- 采购金额
- 采购量
- 加权均价(基本单位)

默认隐藏：

- top5采购金额集中度
- top20采购金额集中度

#### 供应链_收货

默认展示：

- 收货量
- 收货金额
- 库存数量

#### 供应链_物流

默认展示：

- 物流费用总额
- 在途合格数(冷链断链)
- 在途总数(冷链断链)

默认隐藏：

- 配送车次数
- 配送晚点次数
- 配送次数
- 配送门店数
- 配送里程数
- 准时交付率(交付满足率)
- 冷链断链率(车温不合格率)

### 7.5 更多指标入口

当某个指标分组存在未展示指标时，在该分组末尾展示“更多指标”入口。

点击入口后：

1. 打开对应指标分组的更多指标弹窗。
2. 弹窗仅展示当前指标分组下的指标。
3. 用户可以勾选或取消勾选指标。
4. 保存后，指标列表立即更新。
5. 一级快速导航高亮状态重新计算。

## 8. 一级导航视觉状态

### 8.1 默认状态

未选中的一级导航项：

- 文字颜色：黑色
- 小眼睛颜色：黑色
- 节点为普通状态
- 不展示右侧箭头

### 8.2 选中状态

当前选中的一级导航项：

- 文字颜色：原蓝色
- 节点颜色：原蓝色
- 文字右侧展示 `>` 箭头
- 小眼睛仍可点击

### 8.3 隐藏状态

当一级分类被隐藏时：

- 小眼睛展示带斜线的隐藏态
- 分类内容在列表中隐藏
- 导航项仍保留在一级快速导航中
- 分类名称可置灰或降低视觉权重
- 再次点击小眼睛可恢复上次展示内容

## 9. 小眼睛显隐控制需求

### 9.1 基本规则

每个一级导航项右侧都有小眼睛图标，用于控制整个一级分类下内容的隐藏与展示。

小眼睛控制范围：

- 维度侧：控制该一级分类下当前页面展示的维度。
- 指标侧：控制该一级分类下当前页面展示的指标分组和指标项。

### 9.2 隐藏逻辑

用户点击小眼睛隐藏某一级分类时：

1. 系统记录该分类隐藏前实际展示的维度/指标集合。
2. 隐藏该集合中的维度/指标。
3. 该分类未展示的隐藏项继续保持隐藏。
4. 不改变其他一级分类的展示状态。
5. 不关闭或删除“添加更多维度/指标”入口的能力。

### 9.3 恢复逻辑

用户再次点击小眼睛恢复某一级分类时：

1. 系统恢复上一次隐藏前记录的维度/指标集合。
2. 如果上次隐藏前只展示了部分内容，则只恢复这部分内容。
3. 不自动展示该一级分类下全部维度/指标。
4. 该分类下其他隐藏内容仍通过“添加更多维度/指标”弹窗添加。

### 9.4 示例

供应链维度默认只展示：

- 品牌
- 供应商
- 供应链门店
- 物流分类
- 配送类型

用户点击“供应链”右侧小眼睛隐藏后，再次点击恢复，应仍只展示以上 5 个维度。

供应链下的“收货主体、采购组织、温层、物料一级”等维度仍然默认隐藏，不应因为恢复小眼睛而全部展示。

## 10. 展示全部需求

### 10.1 展示全部维度

维度选择区提供“展示全部维度”按钮。

点击后：

1. 展示所有一级分类下的所有维度。
2. 所有维度分类的小眼睛恢复为展示态。
3. 更多维度入口可隐藏或置为无可添加状态。
4. 一级快速导航重新计算当前高亮项。

当所有维度已展示时，按钮置为不可用状态，文案显示为“已展示全部维度”。

### 10.2 展示全部指标

指标选择区提供“展示全部指标”按钮。

点击后：

1. 展示所有一级分类下的所有指标。
2. 所有指标分类的小眼睛恢复为展示态。
3. 更多指标入口可隐藏或置为无可添加状态。
4. 一级快速导航重新计算当前高亮项。

当所有指标已展示时，按钮置为不可用状态，文案显示为“已展示全部指标”。

## 11. 滚动联动需求

### 11.1 维度内部滚动

当用户在维度选择区内部上下滚动时：

1. 系统判断当前视口顶部附近展示的维度所属一级分类。
2. 对应一级导航项自动变为选中状态。
3. 原选中项取消选中状态。
4. 如果当前视口同时展示多个分类，以最靠近视口顶部的可见分类为准。

### 11.2 指标内部滚动

当用户在指标选择区内部上下滚动时：

1. 系统判断当前视口顶部附近展示的指标分组所属一级分类。
2. 对应一级导航项自动变为选中状态。
3. 原选中项取消选中状态。
4. 如果当前视口同时展示多个分类，以最靠近视口顶部的可见分类为准。

### 11.3 页面整体滚动

当选择区处于展开状态，用户滚动整个页面时：

1. 维度一级导航应跟随当前页面中展示的维度内容切换高亮。
2. 指标一级导航应跟随当前页面中展示的指标内容切换高亮。
3. 滚动联动不应因展开状态或页面滚动方式不同而失效。

### 11.4 内容变化后的重新计算

以下操作完成后，系统需要重新计算一级导航高亮：

- 点击一级导航定位
- 点击小眼睛隐藏
- 点击小眼睛恢复
- 点击展示全部维度
- 点击展示全部指标
- 在更多维度弹窗中保存
- 在更多指标弹窗中保存
- 点击恢复默认设置
- 展开或收起选择区

## 12. 展开全部按钮需求

“展开全部”按钮用于控制维度选择区和指标选择区的整体展开/收起。

位置要求：

1. 按钮放在维度选择区和指标选择区中间分割线的底部。
2. 按钮位于指标一级快速导航下方。
3. 按钮不应遮挡维度或指标内容。
4. 页面展开后，按钮位置仍保持在中间分割线附近。

交互要求：

1. 默认状态展示“展开全部”。
2. 点击后选择区展开，按钮文案切换为“收起”或等价文案。
3. 再次点击后选择区恢复默认高度。
4. 展开/收起后需要重新计算一级导航高亮。

## 13. 状态优先级

当多个状态同时存在时，按以下优先级处理：

1. 用户手动隐藏的分类优先保持隐藏。
2. 小眼睛恢复只恢复上次隐藏前的展示集合。
3. “展示全部”优先级高于默认隐藏规则，会展示全量内容。
4. “恢复默认设置”将维度/指标恢复到系统默认展示范围。
5. 更多弹窗保存结果优先更新当前分类的展示集合。
6. 滚动高亮只反映当前可见内容，不改变展示集合。

## 14. 异常与边界场景

1. 某一级分类下所有内容被隐藏时，一级导航仍保留。
2. 某一级分类无可见内容时，点击分类名称不触发无效滚动。
3. 用户隐藏当前高亮分类后，应自动切换到当前视口中下一个可见分类。
4. 用户展示全部后，所有小眼睛应为展示态。
5. 用户恢复默认后，供应链只展示默认维度/指标，不展示全量。
6. 更多弹窗中取消全部内容后，该分类可变为无可见内容。
7. 搜索筛选不应破坏一级分类与维度/指标的归属关系。
8. 指标名称较长时，不能与小眼睛、箭头或正文指标内容重叠。

## 15. 验收标准

1. 维度选择区存在完整一级快速导航。
2. 指标选择区存在完整一级快速导航。
3. 维度侧新增“供应链”和“财务”一级分类。
4. 指标侧新增“供应链”一级分类。
5. “管报三级部门”只归属于维度“财务”分类。
6. 点击一级导航可定位到当前可见的分类内容。
7. 滚动维度列表时，维度一级导航高亮自动变化。
8. 滚动指标列表时，指标一级导航高亮自动变化。
9. 页面整体滚动时，一级导航高亮仍能跟随当前展示内容变化。
10. 选中一级导航为蓝色，并展示右侧 `>`。
11. 未选中一级导航文字为黑色。
12. 小眼睛默认黑色。
13. 小眼睛隐藏分类后，再次点击只恢复上次展示内容。
14. 小眼睛恢复不会强制展示该分类下所有隐藏内容。
15. 维度“展示全部维度”可一键展示所有维度。
16. 指标“展示全部指标”可一键展示所有指标。
17. “添加更多{一级分类名称}维度”文案正确。
18. “更多指标”入口可以打开对应指标分组的指标弹窗。
19. 指标一级快速导航位于维度和指标选择区中间线上。
20. “展开全部”按钮位于指标一级快速导航下方。
21. 快速导航名称、箭头、小眼睛之间布局紧凑且不重叠。

## 16. 埋点建议

如需后续分析用户使用情况，建议增加以下埋点：

- 点击一级快速导航
- 点击小眼睛隐藏分类
- 点击小眼睛恢复分类
- 点击展示全部维度
- 点击展示全部指标
- 点击添加更多维度
- 点击更多指标
- 展开全部/收起

埋点字段建议包括：

- 页面名称
- 区域类型：维度/指标
- 一级分类名称
- 操作类型
- 操作前展示数量
- 操作后展示数量
- 是否为默认展示内容

## 17. 待确认事项

1. 小眼睛隐藏/恢复状态是否需要跨页面刷新保留。
2. 展示全部后的状态是否需要写入用户偏好。
3. 恢复默认设置是否需要同时重置小眼睛状态。
4. 指标分类被全部隐藏时，是否需要展示空状态提示。
5. 一级导航在窄屏或小窗口下是否需要特殊收起策略。
