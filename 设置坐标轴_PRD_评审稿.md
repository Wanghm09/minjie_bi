# 设置坐标轴功能 PRD

## 1. 文档信息

- 功能名称：趋势分析-设置坐标轴
- 所属模块：敏捷自助分析 / 趋势分析
- 文档用途：产品评审、设计评审、研发评审
- 文档版本：V1.0
- 输出时间：2026-04-23

---

## 2. 背景与问题

在趋势分析中，用户可能同时选择多个指标进行折线图分析。不同指标之间常出现以下问题：

- 指标量纲不同
  - 例如金额、人数、桌数、翻台率、占比等混合展示
- 数值量级差异较大
  - 某些指标波动区间极大，导致其他指标曲线被压扁
- 用户希望主动控制指标落在主轴还是次轴
  - 尤其是在多指标联动分析时，需要更清晰地对比趋势

因此，需要提供“设置坐标轴”能力，让用户可以为指标手动指定主轴 / 次轴，提升图表可读性与分析效率。

---

## 3. 产品目标

### 3.1 目标

- 支持用户在趋势分析折线图场景下，主动配置指标显示在主轴或次轴
- 降低多指标混合分析时的阅读成本
- 保证在不同指标类型组合下，图表展示逻辑清晰、结果可预期

### 3.2 非目标

- 不支持为柱状图设置坐标轴
- 不支持在表格模式下设置坐标轴
- 不支持对构成维度模式下的序列逐一设置坐标轴
- 不支持拖拽式排序、批量分组、自动推荐轴位编辑确认弹窗等高级能力

---

## 4. 当前原型行为梳理

基于当前页面原型，现有“设置坐标轴”能力具备以下行为：

### 4.1 入口显示条件

“设置坐标轴”按钮仅在以下条件同时满足时展示：

- 当前图表模式为折线图
- 当前不是构成维度模式
- 当前选中的非构成指标数大于 1

以下情况下按钮隐藏：

- 柱状图模式
- 表格模式
- 构成维度模式
- 仅选择 1 个指标

### 4.2 交互入口

- 按钮名称：`设置坐标轴`
- 点击按钮后，弹出坐标轴配置浮层
- 再次点击按钮，或点击浮层外区域，关闭浮层

### 4.3 浮层结构

浮层当前采用双列表格结构：

- 左列：指标
- 右列：坐标轴

每一行对应一个指标，用户可选：

- 主轴
- 次轴

表现形式为单选按钮。

### 4.4 配置保存逻辑

- 浮层打开时，基于当前已生效配置生成临时编辑态
- 用户在浮层内修改主轴 / 次轴时，只更新临时态
- 用户关闭浮层时，临时态覆盖正式态并生效
- 若关闭前配置发生变化，则刷新趋势图
- 若未发生变化，则不触发刷新

### 4.5 图表生效逻辑

当前图表对左右轴的使用，存在三类模式：

#### A. 用户手动配置轴位

当指标为数值指标，且用户通过“设置坐标轴”指定主轴 / 次轴时，图表按用户配置渲染。

#### B. 系统自动双轴

以下场景即使用户未手动配置，也可能自动启用双轴：

- 数值指标与比例/率类指标混合展示
- 数值指标之间单位类型不同

#### C. 强制右轴场景

在部分模式下，占比类 / 比率类天然使用右轴，例如：

- 构成维度 + 全部视图
- 非构成模式下，率类指标参与混合展示

### 4.6 与其他功能关系

#### 图表样式

- 折线图：支持设置坐标轴
- 柱状图：不支持
- 表格：不支持

#### 数据视图

- 数值视图：支持
- 占比视图：通常无手动配置入口
- 全部视图：受混合双轴逻辑影响

#### 指标/日期倒置

- 该能力仅用于表格模式
- 与“设置坐标轴”无直接联动

---

## 5. 用户角色与使用场景

### 5.1 用户角色

- 区域经营分析人员
- 门店经营分析人员
- 数据分析师
- 业务运营 / 产品运营

### 5.2 典型使用场景

#### 场景 1：金额 + 人数联合分析

用户同时查看“营业收入”和“就餐人数”趋势，希望收入走主轴、人数走次轴，便于观察两者变化关系。

#### 场景 2：金额 + 翻台率联合分析

用户同时查看“营业收入”和“翻台率(整体)”趋势，希望金额用左轴、翻台率用右轴，避免百分比与金额混在一个刻度体系中。

#### 场景 3：多个金额型指标联合分析

用户同时查看“营业收入”“会员收入”“外卖收入”，其中某个指标量级明显偏小，希望人为切到次轴提升可见度。

---

## 6. 功能需求

## 6.1 入口需求

### 6.1.1 展示条件

系统应仅在满足以下条件时展示“设置坐标轴”按钮：

- 当前图表模式 = 折线图
- 当前非构成模式
- 当前可绘制指标数 > 1

### 6.1.2 隐藏条件

出现以下任一条件时，按钮隐藏：

- 当前为柱状图
- 当前为表格
- 当前开启构成维度
- 当前仅有 1 个指标

### 6.1.3 隐藏时处理

- 若按钮因条件不满足而隐藏，已打开的坐标轴浮层应自动关闭

---

## 6.2 浮层需求

### 6.2.1 打开方式

- 点击“设置坐标轴”按钮打开浮层

### 6.2.2 关闭方式

- 再次点击“设置坐标轴”
- 点击浮层外区域

### 6.2.3 浮层内容

浮层应包含：

- 标题行
  - 指标
  - 坐标轴
- 指标列表
- 每个指标对应的轴位单选项
  - 主轴
  - 次轴

### 6.2.4 指标展示规则

浮层内仅展示当前趋势图中可被手动配置轴位的指标。

不应展示：

- 构成维度拆分出来的子序列
- 非当前图表上下文中的隐藏指标

---

## 6.3 配置规则

### 6.3.1 默认规则

当用户未手动配置时：

- 系统按既有自动分轴逻辑出图
- 浮层打开时，默认选中系统当前实际使用的轴位

### 6.3.2 配置粒度

- 配置粒度为“指标”
- 一个指标同一时间只能属于一个轴

### 6.3.3 生效时机

用户在浮层中修改后：

- 可先写入临时态
- 当用户关闭浮层时正式生效

### 6.3.4 保存策略

建议当前版本采用“页面会话级保存”：

- 当前页面内切换日期、维度、图表刷新后可继续保留
- 关闭页面或重新进入不要求永久保存

如后续业务需要，可升级为用户级持久化配置。

---

## 6.4 图表渲染规则

### 6.4.1 单轴场景

当所有指标均落在主轴，且不存在必须启用双轴的系统规则时：

- 图表仅展示左轴

### 6.4.2 双轴场景

当任一指标被分配到次轴，或系统规则要求双轴时：

- 图表展示左右两条 Y 轴
- 左右轴刻度分别独立计算

### 6.4.3 右轴是否展示

仅当右轴存在有效序列时展示右轴；否则不展示。

### 6.4.4 同类指标跨轴

允许同类指标被拆分到左右轴，不做产品限制。

---

## 6.5 与自动分轴逻辑的关系

需要明确“手动设置”和“系统自动”的优先级。

### 6.5.1 优先级原则

- 用户手动设置优先级高于系统默认推荐

### 6.5.2 建议规则

- 若用户显式将某指标设置到主轴/次轴，则图表应按用户选择渲染
- 若用户未显式设置，则按系统规则自动分配

### 6.5.3 系统自动分轴保底逻辑

以下场景系统仍可作为兜底策略：

- 率类指标默认偏向右轴
- 不同单位指标组合时自动双轴

---

## 7. 交互说明

## 7.1 页面交互流程

1. 用户进入趋势分析
2. 选择至少 2 个指标
3. 切换为折线图
4. 点击“设置坐标轴”
5. 系统弹出配置浮层
6. 用户为每个指标选择主轴 / 次轴
7. 用户点击空白区域关闭浮层
8. 配置生效，趋势图刷新

---

## 7.2 异常与边界交互

### 7.2.1 仅一个指标

- 不展示“设置坐标轴”按钮

### 7.2.2 切换到柱状图 / 表格

- 按钮隐藏
- 已打开浮层关闭

### 7.2.3 切换构成维度

- 按钮隐藏
- 已打开浮层关闭

### 7.2.4 指标发生变化

- 若某指标已不在当前趋势图中，其旧配置不应影响当前展示
- 新指标应采用系统默认轴位

### 7.2.5 浮层内连续修改

- 应允许用户连续修改多个指标后一次生效

---

## 8. 页面展示要求

## 8.1 按钮文案

- `设置坐标轴`

## 8.2 浮层字段

- 表头：`指标` / `坐标轴`
- 单选项：`主轴` / `次轴`

## 8.3 布局要求

- 浮层跟随按钮定位
- 浮层最大宽度控制在适配区域内
- 指标名称较长时允许换行或截断，但需保证可识别

---

## 9. 数据与状态设计

建议前端维护以下状态：

- 已生效轴位映射
  - `metric -> left/right`
- 浮层编辑中的临时轴位映射
  - `metric -> left/right`
- 是否有未提交变更

### 9.1 建议状态示例

```json
{
  "trendAxisAssignMap": {
    "营业收入": "left",
    "翻台率(整体)": "right"
  },
  "trendAxisPendingMap": {
    "营业收入": "left",
    "翻台率(整体)": "left"
  },
  "trendAxisDirty": true
}
```

---

## 10. 权限与约束

当前版本默认所有用户可用，不做权限区分。

约束如下：

- 仅折线图支持
- 仅非构成模式支持
- 至少 2 个指标时支持

---

## 11. 埋点建议

建议新增以下埋点，便于评估功能使用效果：

### 11.1 按钮曝光

- 事件：`trend_axis_config_exposure`
- 字段：
  - 页面
  - 指标数
  - 查询粒度

### 11.2 按钮点击

- 事件：`trend_axis_config_click`
- 字段：
  - 页面
  - 指标列表
  - 图表类型

### 11.3 轴位变更

- 事件：`trend_axis_config_change`
- 字段：
  - 指标名
  - 变更前轴位
  - 变更后轴位

### 11.4 配置生效

- 事件：`trend_axis_config_apply`
- 字段：
  - 最终左右轴指标列表
  - 是否触发图表刷新

---

## 12. 验收标准

### 12.1 基础验收

- 选择 2 个及以上指标且为折线图时，展示“设置坐标轴”按钮
- 点击按钮可打开浮层
- 浮层内展示所有可配置指标
- 每个指标可在主轴 / 次轴间切换
- 关闭浮层后图表按配置刷新

### 12.2 隐藏逻辑验收

- 切换到柱状图后按钮隐藏
- 切换到表格后按钮隐藏
- 开启构成维度后按钮隐藏
- 指标数降为 1 后按钮隐藏

### 12.3 渲染验收

- 当存在次轴指标时，图表展示右轴
- 当无次轴指标时，图表可仅展示左轴
- 左右轴刻度按各自序列独立计算
- 手动设置后，图例、曲线、坐标轴对应关系正确

### 12.4 稳定性验收

- 浮层反复打开关闭不报错
- 切换日期、粒度、指标后不出现脏数据串配
- 多次配置后图表渲染正常，无交互失效

---

## 13. 风险与待确认项

以下问题建议在评审中确认：

### 13.1 手动配置是否需要持久化

- 当前建议：仅当前页面会话有效
- 待确认：是否需要按用户记忆

### 13.2 比例类指标是否允许切到主轴

- 当前原型从实现上允许部分率类参与轴位逻辑
- 待确认：产品是否要限制率类只能在右轴

### 13.3 自动分轴与手动分轴冲突时的最终规则

- 当前建议：手动优先
- 待确认：是否存在某些系统强制轴位不可被覆盖

### 13.4 构成维度模式未来是否支持

- 当前不支持
- 若后续支持，需要明确配置对象是“维度拆分序列”还是“基础指标”

### 13.5 是否需要“恢复默认”

- 当前原型未提供
- 建议后续版本考虑增加“恢复系统默认分轴”入口

---

## 14. 后续迭代建议

### 14.1 V1.1

- 增加“恢复默认”按钮
- 增加轴位推荐提示

### 14.2 V1.2

- 支持用户级记忆
- 支持更多单位类型智能推荐

### 14.3 V1.3

- 支持构成维度模式下的轴位配置
- 支持更丰富的图例与轴说明

---

## 15. 评审结论建议

本功能适合作为趋势分析多指标场景下的增强能力上线，建议按以下原则评审：

- 产品侧确认入口显示条件与优先级规则
- 设计侧确认浮层交互与文案
- 研发侧确认状态管理与刷新机制
- 测试侧重点覆盖图表模式切换、指标切换、异常关闭、双轴渲染和边界场景

若评审通过，建议按当前原型逻辑先落地 V1，确保基础可用，再逐步增强持久化、推荐配置与恢复默认能力。
